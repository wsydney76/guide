{% set guide = craft.entries.section('guide').slug(entry.section.handle).one() %}

{% if guide %}

    {% set guideUrl = cpUrl("guide-ajax/#{guide.slug}") %}

    {% embed 'wsguide/modalbutton_ajax.twig' with {id:'showguide', caption:'Guide', url: guideUrl } %}
        {% block content %}
            Loading...
        {% endblock %}
    {% endembed %}

{% endif %}


